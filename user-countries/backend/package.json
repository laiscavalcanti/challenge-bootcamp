{
  "name": "backend",
  "version": "1.0.0",
  "description": "1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server1":"json-server --watch countries.json --port 3001",
    "server2": "json-server --watch users.json --port 3002"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.16.1"
  }
}
